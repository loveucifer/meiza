{
  "name": "mieza",
  "version": "1.0.0",
  "description": "A complete circuit description language system with parser, validator, runtime, and renderer",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc",
    "dev": "tsc --watch",
    "test": "vitest",
    "test:run": "vitest run",
    "lint": "eslint src --ext .ts",
    "prebuild": "rm -rf dist"
  },
  "keywords": ["circuit", "electronics", "parser", "hdl", "cdl"],
  "author": "Qwen",
  "license": "MIT",
  "devDependencies": {
    "@types/node": "^18.11.18",
    "typescript": "^4.9.4",
    "vitest": "^0.28.3",
    "@vitest/coverage-c8": "^0.28.3"
  }
}